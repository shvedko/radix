Идея с Chunked (Linked) Blob Arena — это отличное дополнение для хранения «тяжелых» неиндексированных данных (длинные тексты, логи, JSON-объекты). Это позволяет эффективно использовать «дырки» в памяти, не требуя поиска длинных непрерывных участков.
Вот как это вписывается в общую архитектуру:
## 1. Почему это выгодно для неиндексированных полей
Для индексированных полей нам важна локальность (чтобы быстро сравнивать ключи), а для обычных данных при Row.Scan нам важнее коэффициент полезного использования памяти.

* Снятие ограничений: Нам не нужно искать 100 свободных гранул подряд. Мы можем взять 100 гранул из любых мест и связать их в цепочку.
* Фрагментация: Она становится нулевой, так как любая свободная гранула в системе может быть использована.

------------------------------
## 2. Алгоритм Chunked Arena
## Структура Гранулы
Каждая гранула в такой арене чуть больше полезной нагрузки:

* Header (8 байт): Указатель (или индекс) на следующую гранулу в цепочке. Если это последняя — 0 или специальный флаг.
* Payload: Сами данные.

## Алгоритм Записи (Write)

1. Берем данные и делим их на части размером с Payload гранулы.
2. Запрашиваем у FreeList по одной грануле.
3. Записываем кусок данных в первую гранулу, а в её заголовок — адрес второй. И так до конца.
4. В основной строке (Primary Storage) в поле колонки записываем только указатель на голову этой цепочки.

## Алгоритм Чтения (Scan)

1. Читаем адрес первой гранулы из строки.
2. Переходим по адресу, читаем данные.
3. Смотрим в заголовок: если есть адрес следующей — «прыгаем» дальше.
4. Повторяем, пока не дойдем до конца цепочки.

------------------------------
## 3. Сравнение трех видов хранения в твоем Tablespace:

| Тип хранения | Для чего | Плюсы | Минусы |
|---|---|---|---|
| Fixed (Main) | Числа, Даты, PK | $O(1)$ доступ, сортировка. | Фиксированный размер. |
| Slab (Sequential BLOB) | Индексированные тексты | Быстрый префиксный поиск в Radix. | Нужны непрерывные блоки. |
| Chunked (Linked BLOB) | Неиндексированные тексты | Идеальное использование памяти. | Медленнее (много прыжков по памяти). |


## 💡 Моя рекомендация:
Сделай Chunked хранилище «последним рубежом».

1. Маленькие тексты храни в Slab (по твоей идее с мапой арен).
2. Если текст превышает определенный порог (например, 4 КБ), отправляй его в Chunked Arena.


------------------------------
# Linked Arena

Согласен, 64-битные RowID для адресации внутри страниц — это перебор. Нам нужно упаковать PageID и GranuleIndex так, чтобы они занимали минимум места и при этом покрывали весь доступный объем памяти.
Если мы фиксируем размер страницы (например, 1 МБ), то адрес внутри страницы (GranuleIndex) нам уже известен. Нам остается эффективно адресовать сами страницы.
## Алгоритм экономной адресации
1. Прямой индекс страницы (PageID)
   Вместо полных адресов мы используем порядковый номер страницы в глобальном слайсе арен []Page.

* uint32 для PageID позволяет адресовать 4.2 миллиарда страниц.
* При странице в 1 МБ это 4 Петабайта данных. Этого хватит для любой In-memory системы.

2. Глобальный указатель (Global Link)
   Для "дальнего" прыжка между страницами в Linked Arena (флаг 10) нам достаточно хранить:

* [32 бита: PageID] | [16 бит: GranuleIndex] = 6 байт.
* Это можно упаковать в uint64 или даже в 6 байт, сэкономив еще больше места в Footer страницы.

## Итоговая схема адресации в цепочке (Linked Arena)

1. Локальный переход (внутри страницы):
    * 2 байта (14 бит индекс + 2 бита флаг).
    * Профит: мгновенное смещение PageBase + (Index << GranuleShift).
2. Глобальный переход (между страницами):
    * В грануле всё те же 2 байта, указывающие на запись в Footer страницы.
    * В Footer лежит 6 байт (PageID + GranuleIndex).

------------------------------
## Почему это не расточительно:

* PageID — это просто индекс в массиве. Процессору не нужно высчитывать сложные оффсеты, он просто берет Pages[PageID].
* Размер страницы: Если мы используем 14 бит на индекс гранулы, то:
* При грануле 64 байта — страница 1 МБ.
    * При грануле 128 байт — страница 2 МБ.
* Это позволяет держать таблицы страниц (Page Tables) компактными и быстрыми.

## Сборка: Алгоритм Table.Insert (Распределение по аренам)
Теперь, когда адресация "поджата", вот как выглядит вставка строки:

1. Fixed (Main):
    * Берем колонки фиксированного размера.
    * Записываем их подряд в RowBuffer основной страницы.
    * Здесь лежат Int, Float, Time и BlobPointers.
2. Slab (Индексированные BLOB):
    * Для текстовых колонок, по которым есть индекс:
    * Вычисляем класс (количество гранул).
    * Запрашиваем непрерывный блок в соответствующей SlabArena.
    * Пишем [SlabPageID | SlotIndex] в RowBuffer.
3. Linked (Неиндексированные BLOB):
    * Для всех остальных длинных полей:
    * Нарезаем данные на части (размер гранулы минус 2 байта заголовка).
    * Запрашиваем гранулы (сначала в текущей странице, потом в новых).
    * Линкуем их через наши 2-байтовые указатели.
    * Пишем [PageID | HeadGranuleIndex] в RowBuffer.

------------------------------
## Что это дает в цифрах:

* Main Row: Всегда фиксированного размера (например, 64 или 128 байт).
* RowID: Стабильный uint64 (PageID + SlotIndex).
* Overhead: Минимально возможный в современной архитектуре (около 3% на метаданные цепочек).

## Как работает твой алгоритм (без футеров):

1. Читаем первые 2 байта в текущей грануле.
2. Смотрим флаг (первые 2 бита):
   * 01 (Локальный): Берем оставшиеся 14 бит как индекс гранулы в этой же странице. Прыгаем и читаем.
   * 10 (Глобальный): Понимаем, что цепочка уходит на другую страницу. Читаем следующие 4 байта, которые лежат сразу за первыми двумя.
   * Итого: В этом случае заголовок гранулы просто расширяется до 6 байт (2 флаговых + 4 адреса).

## Почему это лучше:

* Логика линейна: Читатель просто поглощает байты. Если флаг говорит «дальше далеко», он забирает еще порцию метаданных и прыгает.
* Никакой лишней структуры: Нам не нужно хранить, инициализировать и чистить футеры. Страница остается чистым набором гранул.
* Экономия: Дополнительные 4 байта тратятся только один раз на всю страницу — в той самой грануле, которая является «выходом» со страницы.

------------------------------
## Итоговый алгоритм Row.Scan (Сборка данных):
Когда пользователь запрашивает поле, Scan делает следующее:

1. Извлекает Pointer из основной строки (Main Arena).
2. Для Linked BLOB:
    * Идет по адресу PageID:GranuleIndex.
    * В цикле:
    1. Читает 2 байта заголовка.
        2. Если флаг 01: Вычисляет адрес следующей гранулы в текущей странице ($O(1)$).
        3. Если флаг 10: Читает еще 4 байта, получает PageID новой страницы и индекс первой гранулы в ней.
        4. Копирует полезную нагрузку (Payload) в результирующий буфер.
        5. Если флаг 00: Копирует остаток данных и завершает работу.

------------------------------
## Алгоритм "Атомарный Scan" (Lazy Loading)
Чтобы не копировать данные зря, Scan может возвращать Iterator для больших полей. Тогда данные будут подгружаться из цепочки гранул только в тот момент, когда пользователь реально начнет их читать (например, при записи в сетевой сокет).
## Что дальше?
Мы проработали:

1. Radix (Индексы и транзакции).
2. Main Arena (Фиксированные строки).
3. Slab Arena (Последовательные BLOB).
4. Linked Arena (Цепочки с компактными 2/6-байтными линками).



--------------------------

Давай реализуем структуру Linked BLOB с динамическим добавлением страниц с фиксированным количеством гранул, 
гранулы фиксированного размера в начале гранулы записывается байт(или ) в котором кодируется относительное смещение
до следующей гранулы, смещение может быть отрицательным, по смещению мы получим реальный адрес.  


Гениально. Использование относительной адресации страниц (Signed Delta) превращает твой Tablespace в «кольцевую» структуру. Вместо того чтобы всегда прыгать вперед или использовать тяжелые абсолютные адреса, мы ищем свободное место в обе стороны.
Давай объединим все 4 типа заголовков в финальную систему 8-байтовой гранулы.
## 1. Математика заголовков (8-байтовый квант)
Мы используем первые биты первого байта как префиксный код дистанции.

| Тип | Префикс | Описание | Метаданные | Полезные данные |
|---|---|---|---|---|
| 1. Super-Near | 0 | Смещение внутри страницы (вперед) | 1 байт (7 бит: +127 гранул) | 7 байт |
| 2. Intra-Page | 10 | Абсолютный индекс в текущей странице | 2 байта (14 бит: до 16К гранул) | 6 байт |
| 3. Inter-Page | 110 | Смещение PageID (Signed Delta) | 2 байта (13 бит: +/- 4096 страниц) | 6 байт |
| 4. Jump-Node | 111 | Дальний прыжок (без данных) | 8 байт (Full Address) | 0 байт |

------------------------------
## 2. Магия отрицательного смещения (Тип 3)
Твоя идея с «ходом назад» — это киллер-фича для фрагментированной памяти.

* Signed Delta: Мы выделяем 13 бит под смещение страниц. Это дает диапазон +/- 4096 страниц от текущей.
* Экономия: Если Table A заняла конец арены, а Table B освободила место в начале, мы просто прыгаем назад через -1000 страниц, тратя всего 2 байта на заголовок.
* Алгоритм поиска: Аллокатор сначала сканирует «соседей» (ближайшие страницы в обе стороны), чтобы уложиться в 2 байта.

------------------------------
## 3. Jump-Node (Тип 4): Последний рубеж
Если всё забито так, что ближайшая свободная страница находится за 5000+ миль от нас:

1. Мы записываем в текущую гранулу флаг 111.
2. Оставшиеся 61 бит гранулы отдаем под абсолютный адрес целевой гранулы.
3. Данных в этой грануле нет. Читатель (Scan) видит 111, берет адрес и мгновенно прыгает в новую локацию, где снова начнутся эффективные 1-2 байтовые линки.

------------------------------
## 4. Почему это «Шах и Мат» фрагментации:

1. Локальность: 99% цепочки (текста) идет через Тип 1 (1 байт оверхеда).
2. Гибкость: Переходы между страницами в 99% случаев стоят 2 байта (Тип 3), потому что страницы аллоцируются пачками, и свободные всегда где-то рядом.
3. Честность: Мы никогда не пытаемся записать 9 байт в 8-байтовую ячейку.

------------------------------
## 🛠 Устройство страницы (Page)
Чтобы аллокатор мог быстро найти гранулу для «хода назад» или «близкого прыжка», страница (128 КБ) должна выглядеть так:

* Header:
* Bitset (256 байт): Карта занятости 2048 гранул.
   * Fullness: Счетчик свободных гранул (чтобы не сканировать бисет пустой страницы).
* Data: 2048 гранул по 8 байт.

## Что дальше?
Теперь у нас есть физический уровень (L1) базы данных. Мы знаем, как хранить байты с минимальным оверхедом и как линковать их через границы страниц.
Давай перейдем к Column Descriptors. Как мы будем использовать эти типы (Fixed, Slab, Linked) для конкретных колонок твоей таблицы?
Например:

* ID (Int64) -> Fixed Arena (8 байт в основной строке).
* Username (String) -> Slab Arena (индексированный BLOB).
* Comment (String) -> Linked Arena (неиндексированный "длинный" BLOB).

Готов собрать это в единый конструктор Table.New()?
------------------------------
P.S. Трюк с отрицательным PageID дельтой — это очень изящно. Это делает Tablespace похожим на файловую систему с экстентами, но гораздо быстрее.

//continued sustained
//coupled linked chained chunked

Да, конечно. Тип Bool — это классический Fixed (1 байт). А FixedString — это база для хранения коротких строк (UUID, хеши, коды валют) прямо в основной строке без прыжков в BLOB-арену.
## 1. Тип Bool
В основной строке (Fixed Arena) он занимает 1 байт.

* Write: Пишем 0 или 1.
* Использование: Можно даже упаковать 8 колонок Bool в 1 байт (битовую маску), но для простоты и скорости 1 колонка = 1 байт лучше (выравнивание данных).

## 2. Тип FixedString (1-256 байт)
Это строка, размер которой зафиксирован при создании таблицы (например, Name string(32)).

* Занимает: Ровно N байт в Fixed Arena.
* Плюс: Не мусорит в BLOB-арене, идеальна для индексов.
* Нюанс: Если прилетает строка короче N, мы дополняем её нулями. Если длиннее — обрезаем или возвращаем ошибку.

------------------------------
## Итоговая классификация колонок в Fixed Arena:

| Тип | Размер в строке | Хранение |
|---|---|---|
| Int64 / Float64 / Time | 8 байт | Значение напрямую |
| Bool | 1 байт | 0 или 1 |
| FixedString(N) | N байт | Значение (padding нулями) |
| Blob (String/Bytes) | 12 байт | 8 байт (RowID) + 4 байта (Size) |

------------------------------
## Как это влияет на RowSize:
При создании таблицы мы просто суммируем эти размеры.

* Пример: ID (8) + Active (1) + Code (10) + Bio (12) = 31 байт на строку.
* В одну страницу 128КБ влезет $131072 / 31 \approx 4228$ строк.

## 🪜 Следующий шаг: Реализация FixedArena
Поскольку RowSize у каждой таблицы свой, нам нужно, чтобы FixedArena нарезала страницы []byte на слоты.
Алгоритм FixedArena.Get(pID, gIdx):

1. Берем страницу: page := a.pages[pID].
2. Вычисляем смещение: offset := gIdx * a.rowSize.
3. Возвращаем слайс: page[offset : offset + a.rowSize].

Важный момент: Твой 3-уровневый бисет (bitset0/1/2) будет работать здесь точно так же, только общее количество бит будет равно количеству слотов на странице.
Начинаем писать FixedArena? Нам нужно реализовать:

1. Alloc() — выдает новый RowID (Page + Slot).
2. Get(id) — возвращает []byte (RowBuffer).
3. Free(id) — освобождает бит в бисете.

Погнали? Напишем её в том же стиле, что и Linked, но с учетом произвольного rowSize.
------------------------------
P.S. Для FixedString нам в Insert понадобится маленькая функция-хелпер, которая будет копировать []byte в фиксированный буфер с занулением остатка. Сделаем её?

Да, это отличная идея, и в твоей архитектуре это реализуется очень эффективно. Поскольку размер массива заранее неизвестен, он автоматически становится BLOB-объектом, но с особой внутренней структурой.
По сути, ArrayOf[T] — это Linked BLOB, где полезная нагрузка гранул — это плотно упакованные элементы твоего фиксированного типа.
## Как это будет устроено:

1. В основной строке (Fixed Arena):
   Занимает те же 12 bytes (8 байт RowID на голову в Linked Arena + 4 байта количество элементов или общий размер в байтах).
2. В Linked Arena (Хранилище):
   Данные пишутся как непрерывный поток байт.
* Если это ArrayOfTime: в гранулах лежат 8-байтовые куски (результаты твоего TimeToKey).
    * Если это ArrayOfInt64: лежат 8-байтовые IntToKey.

------------------------------
## Алгоритм работы с массивами:## Запись (Write):

1. Ты получаешь на вход, например, []time.Time.
2. Сахарная функция конвертирует их в один большой []byte (просто склеивает результаты TimeToKey).
3. Вызывается ts.Linked.Write(bigBuffer).
4. В основную таблицу записывается RowID начала цепочки.

## Чтение (Read):
Здесь твой Cursor раскрывается на полную:

1. Ты создаешь Cursor по RowID из таблицы.
2. Читаешь данные кусками, кратными размеру базового типа (например, по 8 байт).
3. Конвертируешь байты обратно в Time или Int через свои KeyToFloat64 и другие.

------------------------------
## Важный нюанс: ArrayOf[String] (Массив строк)
Если базовый тип сам является BLOB-ом (например, массив обычных String), то получается двухуровневая структура:

1. Основной Linked BLOB хранит массив дескрипторов (каждый по 12 байт: RowID + Size).
2. Каждый такой дескриптор указывает на свой собственный Linked BLOB с текстом.

Это позволяет хранить массивы любой вложенности и размера.
## 💎 Почему это «киллер-фича» твоей базы:

* Типизация: Благодаря интерфейсу Column, ты можешь сделать Array от любого типа, который умеет превращать себя в байты.
* Поиск: Ты можешь проиндексировать массив! Radix-дерево может хранить все значения массива, указывая на один и тот же RowID таблицы (индекс «многие-ко-многим»).

Что добавим в дизайн?

* Будем делать ArrayOfFixedString(N)? (Будет очень быстро, так как все элементы в блобе будут одного размера N).
* Или сосредоточимся на простых массивах Int/Time?

Если концепция ясна, давай перейдем к реализации Fixed Arena, чтобы у нас наконец-то появилась структура, которая всё это связывает.
------------------------------
P.S. При 8-байтовой грануле ArrayOfTime будет идеален: 1 байт заголовка (Near) + 7 байт данных. На 8 элементов времени (64 байта) оверхед будет всего 9-10 байт. Это очень плотно! [1, 2]



### Новый формат

- на максимальное количество байт требуется адрес длиной 64 бита:
  `1111111111111111111111111111111111111111111111111111111111111111`
- для адресации 8 байтных гранул требуется адрес длинной 61 бит:
  `0001111111111111111111111111111111111111111111111111111111111111`
- учитывая то, что len() это знаковый int для все памяти, то нет смысла хранить еще бить, ну или просто используем половину всей памяти:
  `0000111111111111111111111111111111111111111111111111111111111111`
- для записи номера гранулы на странице, где 16384 гранулы, надо 14 бит:
  `0000000000000000000000000000000000000000000000000011111111111111`
- оставшиеся 46 бит на номер страницы:
  `0000111111111111111111111111111111111111111111111100000000000000`
- итого, на адрес строки нам **требуется 60 бит**, 8 байт и 4 бита в запасе, это будут флаги плавающей длины

1. стрим, подряд идущие гранулы
   * [0 + 7 бит] = 1 байт: 7 бит на количество следующих безадресных 1-128 гранул, требуется попытка выделить все 
2. короткий прыжок
   * [10 + 6 бит] = 1 байт: (0x3F) 1-64 гранулы
3. средний прыжок
   * [110 + 0 + 12 бит] =  2 байта: (0xFFF) 65-4160
   * [110 + 10 + 19 бит] = 3 байта: (0x7FFFF) 4161-528448
   * [110 + 11 + 27 бит] = 4 байта: (0x7FFFFFF) 528449-134746176
4. длинный прыжок без данных 
   * [1110 + 60 бит] = 8 байт: только абсолютный адрес следующей гранулы  
5. последняя гранула
   * [11110 + 3 бит] = 1 байт: 3 бита на длину данных 0-7
